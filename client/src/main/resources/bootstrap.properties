spring.cloud.config.uri=http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
spring.application.name=countries-service
spring.cloud.config.name=countries-service
eureka.client.serviceUrl.defaultZone=http://${EUREKA_SERVER_HOST:localhost}:${EUREKA_SERVER_PORT:8761}/eureka
management.endpoints.web.exposure.include=refresh
management.endpoint.refresh.enabled=true
